# Used only for local edge function testing:
#   supabase functions serve analyze-food --env-file supabase/.env
#
# For the deployed function, set this as a Supabase secret instead — never commit
# a real key here:
#   supabase secrets set GEMINI_API_KEY=your_key_here
GEMINI_API_KEY=
